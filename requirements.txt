# Demo-only dependencies.
#
# The recommender engine, ALS model, content-based model, evaluation
# pipeline, preprocessing outputs, and saved validation results have
# zero dependency on Flask -- it is a thin HTTP wrapper used only by
# this folder. Everything else still lives in `../requirements.txt`.
flask>=3.0

# P2 embedding recall + P4 shadow learned ranker. These are also pulled in
# by the research layer (`../requirements.txt`); pinned here so the product
# demo can run standalone with `--no-kgrec`.
scikit-learn>=1.3
joblib>=1.3
numpy>=1.24
